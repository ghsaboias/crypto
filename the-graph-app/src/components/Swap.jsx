import React, { useEffect } from 'react';

function Swap({ swaps, symbols }) {  
  console.log(swaps);
  const { symbol0, symbol1 } = symbols;
  console.log(symbol0, symbol1);
  return (
    <h1>QQ</h1>
  )
}

export default Swap;
